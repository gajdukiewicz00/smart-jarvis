// Временная заглушка для трея - будет реализована позже без проблемных зависимостей

pub fn init() {
    log::info!("Tray functionality temporarily disabled due to dependency conflicts");
    log::info!("Tray will be implemented with native system APIs in future versions");
}
